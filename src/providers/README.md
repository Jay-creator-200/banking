# React Providers Directory

This directory contains React Context Providers for global state and theme controls.

## Planned Providers:
- `ThemeProvider`: Context for toggling Light and Dark mode.
- `BranchProvider`: Tracks the active banking branch session for multi-branch user scoping.
- `ToastProvider`: Dynamic status notifications trigger.
